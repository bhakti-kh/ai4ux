[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn ai4ux:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/me"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
