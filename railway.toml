[build]
builder = "NIXPACKS"
[deploy]
startCommand = "gunicorn ai4ux:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300"
healthcheckPath = "/login"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3